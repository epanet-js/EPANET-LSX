diff --git a/src/epanet.c b/src/epanet.c
index 9ff507f..d36cdfe 100644
--- a/src/epanet.c
+++ b/src/epanet.c
@@ -22,6 +22,7 @@
 #include "funcs.h"
 #include "text.h"
 #include "enumstxt.h"
+#include "extensions.h"
 
 #ifdef _WIN32
 #define snprintf _snprintf
@@ -195,6 +196,9 @@ int DLLEXPORT EN_init(EN_Project p, const char *rptFile, const char *outFile,
     convertunits(p);
     p->parser.MaxPats = 0;
     p->Openflag = TRUE;
+
+    extensions_init(p);
+
     return errcode;
 }
 
@@ -211,7 +215,9 @@ int DLLEXPORT EN_open(EN_Project p, const char *inpFile, const char *rptFile,
  */
  {
    writewin(p->viewprog, FMT100);
-   return openproject(p, inpFile, rptFile, outFile, FALSE);
+   int result = openproject(p, inpFile, rptFile, outFile, FALSE);
+   extensions_open(p, inpFile);
+   return result;
  }     
 
 int DLLEXPORT EN_openX(EN_Project p, const char *inpFile,
@@ -227,7 +233,9 @@ int DLLEXPORT EN_openX(EN_Project p, const char *inpFile,
  */
 {
     writewin(p->viewprog, FMT100);
-    return openproject(p, inpFile, rptFile, outFile, TRUE);
+    int result = openproject(p, inpFile, rptFile, outFile, TRUE);
+    extensions_open(p, inpFile);
+    return result;
 }    
 
 int DLLEXPORT EN_gettitle(EN_Project p, char *line1, char *line2, char *line3)
@@ -379,6 +387,9 @@ int DLLEXPORT EN_close(EN_Project p)
  **----------------------------------------------------------------
  */
 {
+    // Dispatch extension teardown while the project handle is still valid
+    extensions_free(p);
+
     // Free all project data
     freedata(p);
 
@@ -588,6 +599,9 @@ int DLLEXPORT EN_runH(EN_Project p, long *currentTime)
     if (!p->hydraul.OpenHflag) return 103;
     errcode = runhyd(p, currentTime);
     if (errcode) errmsg(p, errcode);
+
+    extensions_run(p);
+
     return errcode;
 }
 
@@ -607,6 +621,9 @@ int DLLEXPORT EN_nextH(EN_Project p, long *tStep)
     errcode = nexthyd(p, tStep);
     if (errcode) errmsg(p, errcode);
     else if (p->outfile.Saveflag && *tStep == 0) p->outfile.SaveHflag = TRUE;
+
+    extensions_next(p);
+
     return errcode;
 }
 
@@ -626,6 +643,9 @@ int DLLEXPORT EN_closeH(EN_Project p)
       closehyd(p);
   }
   p->hydraul.OpenHflag = FALSE;
+
+  extensions_close(p);
+
   return 0;
 }
 
